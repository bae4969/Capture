# 가벼운 베이스 — 규칙·메모리 중심 단일 에이전트 (lite)

이 작업 디렉토리는 **서브에이전트·상태파일·훅 없이** 메인 에이전트 하나가 `.claude/rules/`(행동 지시)와 `.claude/memory/`(사실 기록) 두 폴더만 들고 동작하는 **가장 가벼운 베이스**다. `01`·`02`·`03`이 멀티에이전트 오케스트레이션(4 서브에이전트 + state/history 영속화 + 훅)이라면, `04`는 그 반대편 — 규칙과 메모리만으로 충분한 작업을 위한 최소 하네스다. 세션 맥락은 별도 상태 파일이 아니라 **git 이력**에서 얻는다.

## 이 베이스가 다른 베이스와 다른 점

- **서브에이전트 없음** — researcher/planner/coder/critic 같은 역할 분리가 없다. 메인이 조사·수정·검증을 직접 한다. 따라서 "coder만 코드 수정"이라는 IMPLEMENT 격리도 없다 — 메인이 Edit/Write를 직접 쓴다.
- **state/history 영속화 없음** — `current.json`·`history/`·Stop 훅이 없다. 세션 맥락은 **git 이력**에서 얻는다([.claude/rules/git_history.md](.claude/rules/git_history.md)).
- **모드·고정 파이프라인 없음** — `/full` 같은 모드도, 4단계 강제도 없다. 요청 성격에 맞춰 메인이 판단해 처리한다.
- **두 폴더 + 진입점만** — `.claude/rules/` + `.claude/memory/` + 본 `CLAUDE.md`. 이식은 이 셋만 복사하면 끝(외부 CLI·인증·플래그 불필요).

## 모든 세션 시작 시 — git 이력으로 맥락 잡기

무거운 상태 파일 대신 **git 이력이 세션 재개의 진실의 원천(SoT)**이다. 세션을 시작하면(또는 맥락이 필요하면):

1. `git log --oneline -10` — 최근 작업 흐름을 읽는다.
2. `git status` — 커밋 안 된 변경(작업하다 만 흔적)이 있는지 확인한다.
3. 진행 중이던 작업이 보이면, 새 요청을 바로 덮지 말고 이어서 할지 한 줄로 확인한다.

자세한 절차·예외(git 미초기화 등)는 [.claude/rules/git_history.md](.claude/rules/git_history.md) 참조.

## 작업 흐름 — 원문요청 → 계획 → 승인 → 진행 → 검증 → 요약

메인은 실제 변경이 필요한 요청을 받으면 **바로 수정하지 않고, 계획을 체크리스트로 세워 사용자 승인을 받은 뒤** 실행한다. 서브에이전트 검증 안전망이 없는 대신, 실행 전에 방향을 사용자와 맞춘다. (SoT: [.claude/rules/planning.md](.claude/rules/planning.md))

1. **원문요청** — 요청을 접수한다. 모호하면 먼저 묻는다(코딩 3원칙 §1). 위치를 모르면 Read/Grep/Glob으로 찾는다.
2. **계획 체크리스트** — 무엇을 어떤 순서로 할지, 각 항목에 검증 방법을 붙인 체크리스트로 세운다(영향 파일·리스크 포함).
3. **사용자 승인** — 체크리스트를 보여주고 승인받는다. **승인 없이는 진행하지 않는다.** (AskUserQuestion 있으면 카드로, 없으면 "이대로 진행 / 수정 / 취소"를 명확히 묻는다.)
4. **계획 진행** — 승인된 체크리스트를 항목별로 실행한다. 계획과 어긋나면 멈추고 승인 단계로 되돌린다.
5. **실행 검증** — 결과가 요청을 충족하는지 검증한다. 테스트·빌드가 있으면 Bash로 돌리고, 없으면 사용자 확인 체크리스트를 남긴다(코딩 3원칙 §3).
6. **요약** — 체크리스트 대비 한 일 + 검증 결과를 요지로. 긴 산출물은 `temp/`에.

- **순수 조회·질의·설명**(변경 없음)은 이 흐름 없이 즉시 답한다.
- **자명한 1줄 수정**(typo 등)은 판단해서 생략할 수 있다(코딩 3원칙 Tradeoff). 애매하면 흐름을 타는 쪽으로.

## rules / memory 두 폴더

사용자의 글로벌 `~/.claude/`와 같은 2폴더 규약을 그대로 쓴다:

| 폴더 | 역할 | 판단 기준 | 인덱스(자동 로딩) |
|---|---|---|---|
| `.claude/rules/` | 행동 지시 | "~해라" / "~하지 마라" | `RULES.md` |
| `.claude/memory/` | 사실 기록 | "~이다" / "~였다" | `MEMORY.md` |

- 새 **행동 규칙**이 생기면 `rules/`에 파일 추가 + `RULES.md`에 한 줄.
- 알아둘 **사실**(프로젝트 상태·설정·참조·결정 배경)이 생기면 `memory/`에 파일 추가 + `MEMORY.md`에 한 줄.
- **코드·git으로 알 수 있는 것**(디렉토리 구조, 커밋 이력)은 memory에 중복 저장하지 않는다. memory는 코드가 말해주지 않는 것만 담는다.

## 코딩 행동 3원칙

모든 코드 변경에 [.claude/rules/coding_principles.md](.claude/rules/coding_principles.md)의 3원칙을 적용한다: 생각하고 코딩 / 단순함 우선 / 목표 주도 실행. 서브에이전트 검증이 없으므로, 메인이 수정 후 스스로 §3(목표 주도) 기준으로 자기 변경을 점검한다.

## temp/ 규약

- `temp/`는 임시 산출물·사용자 자료 공간(gitignore). 긴 보고·문서는 여기에 쓰고 채팅엔 링크·요약만 남긴다.
- 사용자 요청 없이는 `.gitignore`를 수정하지 않는다.

## 소통

- **한국어**로 소통한다.
- 채팅엔 요지·결정 지점만. 긴 본문은 `temp/`에.
